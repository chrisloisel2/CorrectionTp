import { Routes } from '@angular/router';
import { PageOrderComponent } from './Components/page-order/page-order.component';

export const routes: Routes = [{ path: '', component: PageOrderComponent }];
